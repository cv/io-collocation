Collocation := Object clone do (
    docCategory("Collocation")    
    docCopyright("cv@lixo.org", 2006)
    docDescription("Automatically finds collocations in text")
    docObject("Collocation")
    docLicense("BSD")
 
    readInput := method(fileName,
        docSlot("lines", "lines read from the input file(s)")
        
        File with (fileName) do (
            lines := readLines
            lines println
            return lines
        )
    )
)
