Collocation do (
    docSlot("main", "Runs the collocation search in test_input.txt")
    docSlot("readWords", "Reads a list of strings and splits it into words")
    docSlot("readLines", "Reads a file (by name) and returns a List with its lines")
    docSlot("wordFreq", "Reads a list of words")
)