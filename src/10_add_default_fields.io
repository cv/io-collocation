Collocation do (
    fileName := ""
    lines := list()
    words := list()
    frequencies := list()
    
    docSlot("fileName", "File name being read")
    docSlot("lines", "Lines of the file")
    docSlot("words", "Words in the file, lowercased")
    docSlot("frequencies", "Map with word frequencies")
)